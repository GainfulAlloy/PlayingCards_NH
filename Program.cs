namespace PlayingCards_NH
{
    internal class Program
    {
        static void Main(string[] args)
        {

            // creates a new Pack of cards
            // shuffles the cards based on users input
            // deals a specified number of cards
            Testing newTest = new Testing();
            newTest.testing();
        }   
    }
}
